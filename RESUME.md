# 引継ぎ（2026-09-05 — 画面はぜんぶ移した）

この節を最初に読めば続きから入れる。**原作の画面はぜんぶ動く**:
**Bio ロゴ（0x10）→ 海のタイトル（0x1e）→ 海 → 空 → 宇宙 → 大物 →
また海（面番号だけ増える）→ ゲームオーバー → 名前入力 → 得点表 →
タイトル**。それにデバッグ メニューからしか出られない
**エンディング → CAST → スタッフロール**。

user の依頼: 「super depth の windows 版を ghidra で解析して wasm 版を
作って」。素材は `depth-build115.exe` 一つだけだった。

## いまの状態

* `python tools/unpack.py` で **45 ファイルが `disk/` に出る**（三層とも
  解いてある。[docs/format.md](docs/format.md)）。インストーラは走らせない
* Ghidra 12.1.3 で `disk/superdepth.exe` を解析済み。作り直しは下記。
  **`MakeThunkFuncs.java` を先に流すこと** —— 0x401005 の JMP 表の
  飛び先は誰も直接呼んでいない（関数ポインタ経由）ので、素の解析では
  関数にならず C に出てこない（メニューの `FUN_00414920` と
  記録画面の `FUN_00414b00` がそれだった）
* `out/superdepth.c` 606 関数（**落ちた関数は 0**）、
  `out/superdepth.asm` 47311 命令。32bit の MSVC なので**逆コンパイルの
  質がとても良い** —— PC-98 の 16bit とは別世界で、ほぼそのまま読める
* **`.dar` は読める**（`src/dar.c`、検査つき）。**描画層もある**
  （`src/video.c`：640x480 の 8bpp、パターン、原作のフォント）
* **BGM は鳴る**（`src/smf.c` / `src/synth.c`。user 自身の
  [windepth_wasm](https://github.com/yomei-o/windepth_wasm) から）
* **面は 4 種類とも動き、輪になって回る**: 海（`src/play.c`）→
  空（`src/air.c`）→ 宇宙（`src/space.c`、台本は `stage3.bin`）→
  大物（`src/boss.c`）→ 面番号が 1 に戻って海へ
* **状態機械が動く**（`src/game.c`）。0x0a → 0x10（Bio ロゴ）→ 0x1e
  （海のタイトル、メニュー・スタッフの流れる字・泳ぐ潜水艦）→
  Game Start で 0x32、放置 1 分でデモ（0x33）、Exit で 0x5a。
  Record で記録画面（`FUN_00414b00`）
* **ゲーム本体が動く**（`src/play.c`、`src/play.h`）。艦・爆雷・敵 6 種・
  魚雷・砲弾・アイテム・連鎖・得点・得点ポップアップ・レーダー・
  ステータス表示・面クリアの演出・ポーズ・ゲームオーバーの名前入力・
  録画デモの再生まで。検査は `tmp/play_check.exe`
* **2 面目（空の面）も動く**（`src/air.c`）。海の面をクリアすると
  カメラが海面まで上がり、そのまま空の面へ。艦は下で上に撃ち、
  飛行機 5 種が爆弾を落とす。クリアすると艦がロケットで宇宙へ上がる
  演出（`FUN_0040f490`）まで。検査は `tmp/air_check.exe`
* 検査は 3 本とも `sh tools/build.sh check` で走る（PNG も出る）
* **WASM とページもある**（`index.html` / `superdepth.js`）。ゲームが
  そのまま動き、`0`/`1`/`2` で展示画面にも切り替わる:
  https://yomei-o.github.io/super_depth_win_wasm/
* **エンディングの 3 画面も動く**（`src/ending.c`）: 地球
  （`FUN_00408650`）→ CAST（`FUN_00408a80`）→ スタッフロール
  （`FUN_00414210`）→ 名前入力。**原作でもここへは
  デバッグ メニューからしか入れない**（下の「メニュー資源」）
* **まだ無いもの**: SOUND TEST（0x46）だけ。**入口が binary 中に無い**
  （`FUN_00421da0(0x46)` を呼ぶ所が無く、デバッグ メニューにも項目が
  ない）ので、移植しない
* 原作にあって呼ぶ所が無い（＝ dead code。移植しない）:
  * `FUN_0040a630` —— アイテム名を 90 フレーム出す表示。`FUN_0040a540`
    が `DAT_004a8538 = 0x5a` と名前番号を立てるが、**それを描く
    `FUN_0040a630` を呼ぶ所が無い**
  * `FUN_00405b70`（飛んでいる爆雷を数える）、`FUN_00409210`
    （拡大縮小、クリップつき）、`FUN_0040bcf0`（bgm08 の別画面）、
    `FUN_00402ac0` / `FUN_00402cb0`（別の文字描画）
  * `_DAT_00461338`（命令 0x849/0x84a で 0/1 になる）も読む所が無い

## 本体の見取り（`disk/superdepth.exe`）

PE32 / MSVC 6.0 / image base 0x400000 / entry 0x426b04（RVA 0x26b04）。
.text 242347、.data は仮想 848552（実体 36864 = 大きな BSS）、
.rsrc 20704、.reloc あり。

**移植の道が見えている部分:**

| | |
|---|---|
| 画面 | GDI の DIB。`CreateDIBitmap` `StretchDIBits` `SetDIBitsToDevice` `BitBlt` `CreatePalette` `RealizePalette` `GetSystemPaletteEntries`。**256 色のパレット画面**で、canvas に素直に乗る |
| 音 | `sndPlaySoundA`（WAV）と `mciSendCommandA`（MIDI）。`WINMM` の `midiOutGetNumDevs` `waveOutGetNumDevs` で有無を見る |
| 時間 | `timeGetTime` |
| 入力 | `GetAsyncKeyState`、`joyGetPos` / `joyGetDevCaps`（ジョイスティック） |
| 設定 | レジストリ `Software\Bio_100%\SuperDepth`、`rank%d` に得点表 |
| DirectX | **import に無い。`LoadLibraryA` + `GetProcAddress` で動的**。readme が「DirectX 3 以降が入っていればフルスクリーンと DirectSound」と書いている。**基準は GDI の道** |

自分の層に名前が付いている: `WinGL(%d-%d)` `WinGLScreenMode(%d)` が描画層、
`MIDI Sound Driver - SMFDrv` と `BMIDIPLAY_*` が音、`Pat*` がパターン
（`.dar`）。窓のクラス名は `SimpleWindow`。

デバッグ表示の書式が残っている: `%2dF SE:%s BGM:%s`、
`rankin = %d rcurX = %02d rcurY = %02d`、`Super Depth Top Score Ranking`。

## 本体の作り（読めたところ）

### 毎フレームの流れ — `FUN_004209a0`

窓オブジェクトの毎フレーム処理。WinGL の側。

```
FUN_00430f50(pad, out)        キー表を舐めて pad のビットを作る（下記）
  → DAT_004bf83c.. に 1 ビットずつばらす
GetLocalTime / GetCursorPos   → これも global へ
thunk_FUN_00401500(surface)   ★ゲームの 1 フレーム（下記）
FUN_00422410 / FUN_00425f80
画面の端 2 本を消す
必要なら解像度を変える        DAT_004bf8bc: 0=640x480 1=320x240 2=1280x960
FUN_00417e70(surface, hdc)    ★present（DC へ転送）
DAT_004bf820 = (frame+1) & 0x800000ff   ★フレーム数
DAT_004bf810[0xd7] を DAT_004bfc18 へ複写   ★前フレームの入力（エッジ検出用）
```

**タイマは 33ms**（`WinMain` の `FUN_00424870(this, 0x21, 200, 0)`。
第 2 引数 200 は取りこぼしの追いつき上限。windepth は 50ms=20fps だった）。

### pad のビットとキー — `DAT_00444c20` の表（実測）

| ビット | 既定のキー |
|---|---|
| 0x01 UP | K / ↑ / テンキー8 |
| 0x02 DOWN | J / ↓ / テンキー2 |
| 0x04 LEFT | H / ← / テンキー4 |
| 0x08 RIGHT | L / → / テンキー6 |
| 0x10 BTN1 | Z / Space（2P はテンキー0） |
| 0x20 BTN2 | X / Enter（2P はテンキー5） |
| 0x40 BTN3 | Shift |
| 0x80 BTN4 | Q |
| 0x1000 START | F2 |
| 0x8000 | Esc（表の外、直接 GetAsyncKeyState） |

**windepth_wasm と同じ並び**（同じ WinGL なので当然）。あちらの
`index.html` の KEYMAP がそのまま使える。

### ゲームの状態機械 — `FUN_00401500`

`switch (DAT_004bf894)` ひとつ。`FUN_00421da0(n)` が次の状態を入れ、
`DAT_004bf89c` が「入ったばかり」の旗。

| 状態 | 中身 |
|---|---|
| 0x0a | 初期化して 0x10 へ |
| 0x0f / 0x10 | **Bio_100% のロゴ**。`staff.dar` を 291 枚読み、`bgm01`、184 行の配列 `DAT_0044653c` を乱数で 0/1 にしてロゴを行ごとに出し入れする（`DAT_004492c0` が 0=出す 2=消す）。消し終わりで 0x1e へ |
| 0x1e | **タイトル（海）**。`depth1.dar` を 9 枚読み（**同じスロット 0xb47 に上書き**）、`bgm02`、空・海面・海中のタイルを敷き、9 枠の潜水艦を 7 レーンに流し、スタッフの字を流す。メニューは overlay hook（下記）。`DAT_00449284 = 0x708` は**減らす所が無い**（死んでいる）|
| 0x32 / 0x33 / 0x34 / 0x35 | タイトルの続き。0x35 は 0x10 へ戻る（デモの輪） |
| 0x46 | **SOUND TEST**（"SOUND TEST" / "Bio_100% LOGO" / "BGM %02d" / `bgm%02d`） |
| 0x5a | 版とクレジット（"Super Depth ver1.00a Copyright(c)1991 Hideki Mori and Yasuo Futatsugi" / "alty & tacox / Bio_100%"） |

`DAT_004492f0` の内側の switch と `"Game Design - alty & tacox"` から
0x25 バイト刻みの表を引く所がスタッフロールの文字。

### パターンのスロットは使い回す

`FUN_00419700`（`PatEntryDAR`）の第 1 引数が**入れる先のスロット番号**で、
ゲームは

* `depth.dar` の 2887 枚を 0..2886 に
* その上の **0xb47 = 2887 から**、場面ごとに `staff.dar`（291 枚）や
  `depth1.dar`（9 枚）を**上書きして**使う

だから状態 0x0f では 0xb47 が `biologo_staff`、状態 0x1e では 0xb47 が
`sea01` になる。**同じ番号が場面によって別の絵**という前提で読むこと。

### パレットは場面の書庫のもの（実測）

画面のパレットは `thunk_FUN_004033a0` の最後の
`FUN_004178e0(surface, 0x280, 0x1e0, 0xb48, ...)` で決まる。0xb48 は
**場面の書庫の 2 枚目**なので、画面のパレットは depth.dar のものでは
なく `staff.dar` / `depth1.dar` のものになる。矛盾しないのは切り分けが
あるから —— 3 つの書庫を突き合わせると

| 番号 | |
|---|---|
| 0..118 | どの書庫でも同一（VGA の 20 色＋灰の階調） |
| 119..245 | **書庫ごとに違う**。場面の絵の専用色 |
| 246..255 | どの書庫でも同一（原色 8 色） |

で、**depth.dar の 2887 枚は 119..245 を 1 バイトも使っていない**
（92.8% が 119 未満、7.2% が 246 以上、間はゼロ）。だから 8bpp の面
一枚と場面のパレット一つで正しく出る。真色の画面では WinGL が
パターンごとに自分のパレットで展開する（`FUN_00414f60` の case 1）が、
結果は同じになる。

### 毎フレームの尾部（switch の後、どの状態でも走る）

1. `DAT_0046217c` が残っていれば**奇数フレームだけ画面全体を 0xff**に
   して減らす（爆発の白フラッシュ。7 や 6 が入る）
2. `DAT_004492cc` があれば消す —— **これを 1 にする所は binary 中に無い**
3. **枠**: `DAT_004bf8b8`（レジストリの FullScreen、既定 0）が 0 なら
   パターン **0x9d9 を四辺に敷き詰める**（刻みは 0x9c9 の大きさ =
   32x32）。フルスクリーンなら同じ場所を黒で塗る。
   **これが `FUN_00409000` の `y + 0x20` の理由** —— 上下左右 32 px は枠
4. `DAT_004bf898`（その状態に入ってからのフレーム数）を増やす
5. `"%2dFPS"` を 8x8 フォントで (col 0x4b, row 0) に出す。**条件なし**。
   ベータなので出しっぱなしになっている

### パターンの大きさは `0x470bd8` の表（0x34 バイト刻み）

`FUN_00409000` が `*(short *)(&DAT_00470bd8 + pat * 0x34)` を幅、
`+2` を高さとして読む。だから逆コンパイルに出てくる**謎の絶対番地は
だいたいこの表の中**で、番号に直せる:

| 番地 | = |
|---|---|
| `DAT_00495644` / `DAT_00495646` | パターン 0xb47（= `EXT_BASE`）の幅・高さ |
| `DAT_004956ae` | 0xb49（staff.dar `kgfwhite`）の高さ = 33 |
| `DAT_00496a2e` | 0xba9（`kgfred`）の高さ |
| `DAT_00497dae` | 0xc09（`kgfyellow`）の高さ |

`(番地 - 0x470bda) / 0x34` がパターン番号。移植では
`vid_pat_info(v, pat)->w/h` がこの表。

### レーダーは hook ごとに別の枝（`FUN_004097a0`）

一つの関数の中に**枝が 5 つ**あって、条件も絵も違う。まとめて
書いてはいけない（最初はまとめて書いていた）:

| hook | 出す条件 | 種類ごとの絵 |
|---|---|---|
| 海 | `y > 0xf` かつ 生きている | 1→0xb41 二つ、2→0xb42 二つ、3→0xb43 一つ、4→0xb44 二つ、5→0xb45 二つ、9→奇数フレームだけ 0xb40 一つ |
| 空 | `y >= -0x1f` | 1→0xb41 **一つ**、2→0xb42 二つ、3→0xb43 **二つ**、4→0xb44 二つ、5→0xb45 **一つ**、9→同上 |
| 宇宙 | `-0x1f <= y <= 0x15f` | 1→0xb41（`[20] <= 4` なら 4 フレームに 1 回）、2/3→0xb40 二つ、4→0xb42 二つ（`[23]` が 2/3 でなければ 4 フレームに 1 回）、5→0xb42 **四つ**（2x2）、6→0xb41、7→0xb42、8/10→0xb45、0x14/0x15（弾）→出さない、その他→奇数フレームに 0xb40 |
| 大物 | `DAT_00461330` かつ `面/4 == 1` | 0xb42 を 4 列 x 3 行（行の y が 0x161..0x199 の間だけ） |
| エンディング | 常に | 敵枠 0 に 0xb48 一つ |

自機（`FUN_004096e0`）は `残機 > 9 かつ y >= 0` のとき、**または
hook がエンディングのとき**に 0xb40 を二つ。

### 面の並び（hook が数珠つなぎになっている）

状態は 0x32 のままで、**中身は `DAT_004492c8` の hook が切り替わる**。

| hook | 中身 | 次 |
|---|---|---|
| `LAB_00401168` = `FUN_00405c10` | 海の面（爆雷） | クリアで `LAB_00401235` |
| `LAB_00401235` = `FUN_00408210` | 海面まで上がる演出 | `LAB_004011ae` |
| `LAB_004011ae` = `FUN_0040c9e0` | 空の面（上に撃つ） | クリアで `LAB_004010d2` |
| `LAB_004010d2` = `FUN_0040f490` | ロケットで宇宙へ上がる演出 | `LAB_0040110e` |
| `LAB_0040110e` = `FUN_0040f970` | 宇宙の面（台本 `stage3.bin`） | EMERGENCY で `LAB_004011c7` |
| `LAB_004011c7` = `FUN_00403dc0` | 大物の面 | 倒すと面番号 1 で `LAB_00401168` |
| `LAB_004011b3` = `FUN_0040bdb0` | ゲームオーバーと名前入力 | タイトル |
| `LAB_00401041` = `FUN_0040b960` | ポーズ | 元の hook |
| `LAB_0040113b` = `FUN_00408650` | エンディング（地球） | 150 フレームで 0x401136 |
| `0x401136` = `FUN_00408a80` | CAST（生き物 20 匹） | 20 匹目で `LAB_00401091` |
| `LAB_00401091` = `FUN_00414210` | スタッフロール | 上まで流れたら `LAB_004011b3` |

面番号は演出のたびに 1 つ増える。`DAT_00462198`（= (面-1)%4+1）が
得点表の行と背景を選ぶので、**4 面で一巡**する作り。

### 入れなかった状態、届かない状態

* **0x46（SOUND TEST）には入れない。** `FUN_00421da0` を 0x46 で呼ぶ所が
  binary 中に無い（状態を変えるのはこの関数だけ）。だから移植もしていない
* **0x0f には遊んでいる限り入れない。** ロゴは 0x10 から入る（中身は
  同じ case）。0x0f はデバッグ メニューの「Bio_100% ロゴ」専用
  （命令 0x86d → `FUN_00421da0(0x0f)`）

### メニュー資源とデバッグ命令（実測 —— エンディングの入口はこれ）

窓には**メニューバーがある**。`FUN_00421120` が窓クラスを
`lpszMenuName = "menu_release"` で登録するので、付くのは資源
`MENU_RELEASE` の方 —— ゲーム / オプション / ヘルプ / インターネット
だけ。**資源にはもう一方 `MENU` も入っていて、そちらには デバッグ が
ある**（`python tools/dar.py` ではなく PE の資源を読めば出る。
種類 4 = RT_MENU、言語 1041）:

```
デバッグ ─ モードセレクト ─ Bio_100% ロゴ     0x86d → 状態 0x0f
         │                タイトル画面        0x86e → 状態 0x1e
         │                エンディング        0x86f → hook 0x40113b + 状態 0x32
         │                スタッフロール      0x870 → hook 0x401091 + 状態 0x32
         ├ ステージセレクト STAGE 01..12      0x84e 0x84f 0x850 0x851 0x852
         │                                   0x853 0x872..0x877
         ├ フルパワー                         0x85f → アイテム 6 を当てる
         ├ デモ再生                           0x87b → hook 海 + 状態 0x33
         ├ デモ録画開始                       0x878 → hook 海 + 状態 0x34
         └ 画面をクリップボードへ転送           0x881
```

資源の文字は UTF-16LE。**cp932 の端末にそのまま出すと化けるので、
UTF-8 のファイルに書いて読むこと。**`MENU` の全体（項目 = 命令番号）:

```
ゲーム       リセット 0x860 / フルスクリーン 0x7f3 /
             オンラインアップデート 0x85c / 終了 0x7d2
オプション   音楽 0x85d / 効果音 0x85e / 効果音をステレオ 0x864 /
             死なない 0x867 / 誘爆 0x86a
ヘルプ       取扱説明書 0x885 / SuperDepth について（開発スタッフ版）
インターネット Bio_100% 0x882 / SuperDepth 0x883 / フィードバック 0x884
デバッグ     モードセレクト・ステージセレクト・フルパワー 0x85f /
             デモ再生 0x87b / デモ録画（＊＊＊ 警告 ＊＊＊… 開始 0x878）/
             画面をクリップボードへ転送 0x881
```

**「誘爆」（0x86a）が `DAT_00463da8`、「死なない」（0x867）が
`DAT_00463dc4`。** どちらも `FUN_00402480` が起動時に入れて誰も戻さない
（誘爆は 1、死なない は 0）。移植では `Game` の欄。

`MENU_RELEASE`（実際に付く方）にあるのは ゲーム、オプション（音楽と
効果音だけ）、ヘルプ、インターネット。
**リセット（0x860）だけは移植した**（状態 0x0a に戻して BGM を
モード 4 で止める）。フルスクリーンと終了とオンラインアップデートは
ブラウザに移す意味がないので入れていない。

受ける側は `0x4260d0` からの WM_COMMAND 振り分け（Ghidra は関数と
見なさない）。`MOVSX EAX,[ESP+0xc]` の命令番号から `0x7d2` を引き、
`0x426804` の byte 表で `0x426764` の飛び先表を引く。**だから
0x33 = デモ再生、0x34 = デモ録画**とここで確定した。

**面選びの対応**（`stage % 4`）: 1/5/9 = 海、2/6/10 = 空、
3/7/11 = 宇宙、4/8/12 = 大物。移植は `game_debug(g, cmd)`
（`src/game.c`）で、ページの `<details>` にボタンとして並べてある。

命令が状態を変えると `FUN_00421da0` が「入った」印を立てるので、
**0x32 の入口処理が後から走って命令の書いた値を上書きする**。
エンディングの命令が入れる面番号 2・自機 (0x120,0x60)・星の初期化は
`FUN_0040b250` と続く 7 行に消されて、面番号 1・自機 (0,0)・星は
ぜんぶ (0,0) の kind 0 になる。**原作がそうなので、そのまま。**

`FUN_0040b250`（0x32/0x33/0x34 の入口だけが呼ぶ）は枠の配列を
9 か所ぶん 0 にしたあと、**pad の展開値とその前フレームの控え、そして
フレーム数 `DAT_004bf820` まで 0 にする** —— 遊び始めはアニメの位相が
必ず 0 から。

### overlay hook —— メニューは状態機械の外にいる

`FUN_004148f0(fn, 1)` が `DAT_004bf164` に関数を入れ、各状態の最後の
`(*DAT_004bf164)()` がそれを呼ぶ。0x401100 からの thunk 表を引くと

| 入る値 | 中身 |
|---|---|
| `LAB_0040118b` | `FUN_00414920` = **タイトルのメニュー**（3 項目） |
| `LAB_004010af` | `FUN_00414b00` = 記録画面（未移植） |

`FUN_00414920` は上下でカーソル、`FUN_00402de0`（BTN1）か
`FUN_00402ec0`（START）で決定、`FUN_00402800` で **SUPER DEPTH の
ロゴをタイル 16x16 で組み立てて**描き、項目を 3 行書き、
**`DAT_004bf16c` を数えて 0x708（59 秒）でデモ（0x33）へ**。
項目は `DAT_00441d68` の 3 本（" Game Start " / "   Record   " /
"    Exit    "）。選択中は 0x580（黄）、それ以外は 0x881（黒）。

決定の判定は **BTN1 を先に見て、押されていなければ START を見る**。
どちらも乱数を 1 個捨てるので、順番を変えると乱数列がずれる。

### 記録画面（`FUN_00414b00`）と得点表

得点表は `DAT_004bf9dc` から **0x28 バイト x 10 本**:

| | |
|---|---|
| +0x00 | 得点（表示は `"%05d0"` なので 10 倍して見せる） |
| +0x04 | 名前 char[16] |
| +0x14 | 日付 char[16] |
| +0x24 | 面（`"%02d"`） |

1 本がそのままレジストリの 1 値（`HKCU\Software\Bio_100%\SuperDepth`
の `rank%d`、`FUN_004026f0` が 0x28 バイトを丸ごと読み書き）。
既定は `FUN_00402610`: 得点 100,90,..,10 / 名前 "Bio_100%" /
日付 "--/--/--" / 面 1。

画面（`FUN_00414b00`）は見出しを赤（0x280）で、表を白（0x180）で書く。
見出しの `" ** Score ****  Name     Date   "` は**実行時に 1,2 と
10..13 の `*` を字 0x15..0x1a に差し替える** —— その字は
「Ra」「nk」「St」「ag」… と 2 文字分が 1 枚に入っている。
行は `FUN_0040bbb0`: 順位の数字（`0x30+順位`、10 位だけ 0x14）と
0x10..0x13 の飾り（3 位までで止まる）、得点・面・名前・日付。
下の `"Hit any key to return."` は `frame & 0xf` が 8 未満のとき。
戻りは **BTN1 の生のエッジ**（`FUN_00402de0` を通らないので乱数を
捨てない）。`FUN_004148f0(&LAB_0040118b, 0)` の **0** が効いて、
戻ったメニューはカーソルを覚えている。

### 波打つ絵（`FUN_004092a0` → `FUN_0041bad0`）

名前入力の背景（space.dar の SPACE1 / SPACE2）は**行ごとに横へずらして**
描く。ずれは `0x442178` の 256 バイトの正弦表（±127、頂上が少し平ら）で

```
行 r のずれ = 表[(phase + (r * 0x200) / wave) & 0xff] * amp >> 7
```

`wave` が負だと **amp の符号が 1 行ごとに反転**する（呼び出しは
wave = -0x100、amp = 0x10、phase = フレーム数）。行数は**画面から
切れていない最初の行から**数える。`src/video.c` の `vid_pat_wave` が
これで、表は binary から取ったものをそのまま置いてある。

### 得点表への書き込み（`FUN_0040bdb0`）

得点が 10 位に届かなければ何も聞かずにロゴへ戻る。届いたら
`space.dar`（50 枚）を 0xb47 に載せ、bgm08、星 256 個（depth.dar の
0xb2e..0xb3d、1x1 の点が 16 色）、3 行 x 32 マスの文字盤
（`0x44055c`、3 行目は DEL / DUP / END だけ）。名前は 8 文字で、
8 文字目を置くとカーソルが END へ飛ぶ。DUP は**表にある名前を
重複なく集めた一覧**を順に貼る。END で表を 1 つずつ下へずらして
書き込み、`DAT_004492cc` を立てて（そのフレームの最後に画面を消す）
タイトルへ。日付は `"%02d/%02d/%02d"` の 年%100/月/日。

### 効果音の名前表（`PTR_DAT_0044277c`）

`FUN_0041fd00(name)` はこの 12 本から名前で引く:
drop, burn, eneshot, item, depth01..06, plane, finalt09。
**`plane.wav` はインストーラに入っていない**（`depth-build115.exe`
の中に "plane" という文字列すら無い）。WinDepth 側には
ある音なので、エンジンごと持ってきた名前が残っているのだと思われる。

### BGM のモード（`FUN_00420980(mode, name)`）

`0..3` が再生、`4` が停止。2 と 3 は**同じ曲なら鳴らし直さない**
（面の途中で復活しても曲が頭に戻らない）。呼び出し側は 0x10 が
`(0, bgm01)`、0x1e が `(1, bgm02)`、面が `(3, bgm03/bgm06)`、
SOUND TEST が `(1, bgm%02d)`。**モードのどれが loop なのかは SMFDrv を
読んでいない。** bgm01 が 3.8 秒のジングルで 0、bgm02 が 43 秒の
タイトル曲で 1 なので「0 は一回だけ、他は loop」と読んで実装した。
これは呼び出し側からの読みで、ドライバから確かめた事実ではない。

## ゲーム本体（`src/play.c` = `FUN_00405c10`）

**オブジェクトは全部 28 int（0x70 バイト）の同じ形**で、使う欄だけ違う。

| 配列 | 場所 | 数 | 空きの印 |
|---|---|---|---|
| 敵 | `DAT_004621a8` | 64（面は先頭 `nenemy`=10 だけ） | y == 0 |
| 爆雷 | `DAT_004a5490` | 16 | y == 0x134（海底） |
| 魚雷 | `DAT_004a4d88` | 16 | y <= 0x20 |
| 砲弾 | `DAT_004a5b90` | 8 | y < -0xf |
| しぶき | `DAT_004a5fa4` | 64 | frame == 4 |

**面の敵は表から**: `0x43fae8 + 面 * 0x40` から **64 dword 読む**ので行が
重なる。面 1 は `1 2 3 1 5 5 1 1 9 9 ...` で、使うのは先頭 10 個。
kind 8 と 12 は**コードが無い** —— readme の「5 面以降は遊べない面ばかり」
はこれ。

**敵の種類**（switch の case）:

| kind | 絵 | 動き |
|---|---|---|
| 1 | 0xa1d / 0xa15 | 7 レーン、速さ 0..3、魚雷を撃つ |
| 2 | 0xa0d | 深い 2 レーン、砲弾を撃つ |
| 3 | 0x9cd | 浅い 2 レーン、小さい、魚雷 |
| 4 | 0xa05+`{5,13,21,29,37}` | 海底の船。止まって狙い（aim 0..4）、4 発まとめて撃つ |
| 5 | 0xa0b + anim*8 | 3 コマの animation、魚雷 |
| 9 | 0x9c9 | 狙いの深さへ vy を ±1 ずつ（減衰なしで上下に揺れる） |

**倒すと**: 速度を半分にして state 9 から 0 へ（爆発 8 コマ 0xa35）、
得点は `0x43fe70[cycle*0x1e + kind]`（面 1 なら kind1=5, 2=30, 3=20,
4=50, 5=30, 9=10）× 連鎖数。爆発中（state 4..8）の敵は触れた敵も
巻き込む（`DAT_00463da8` が立っているとき）。

**アイテム**は kind 9 を倒したときだけ落ちる。16 面ダイス
（`0x4400c8`）を振ってから、持っている物に応じて何段も差し替える
（`FUN_0040aed0`）。中身は Speed Up / Shot Max Up / Shot Power Up /
Flash Bomb / Shot Special / Full Power / Ship 1up。

**当たり判定**は種類ごとに箱が違う（kind 1/2/4/5 は横 0x38、3/9 は 0x18）。
**魚雷は艦の下にいる間しか素直に上がってこない** —— 艦より右に外れて
いて深さ 0x30 より下だと 4 分の 1 の確率で足を止める。

**乱数はゲームの一部**。`FUN_0042691c` は MSVC の rand で、種は 1 固定。
空きスロットでも毎フレーム 1 個引く、キー判定 1 回ごとに 1 個引く、
決定キーは短絡する —— この順番が崩れると別のゲームになる。

## 次にやること（順）

1. ~~`.dar` の展開~~ 済み（`src/dar.c`、docs/format.md）
2. ~~描画層~~ 済み（`src/video.c`）
3. ~~BGM~~ 済み（`src/smf.c` / `src/synth.c` を windepth_wasm から）
4. ~~WASM とページ~~ 済み（いまは絵と音の展示）
5. ~~状態 0x0f/0x10（Bio ロゴ）~~ 済み（`src/game.c`、`tests/logo_check.c`）。
   184 行を 4 行ずつ、0x78 フレーム待って 4 行ずつ消す。乱数は MSVC の
   `rand()`（種は 1 固定なので毎回同じ順）
6. ~~状態 0x1e（海のタイトル）~~ 済み（`src/game.c`、`tests/title_check.c`）。
   メニュー（`FUN_00414920`）と タイル組み立てのロゴ（`FUN_00402800`）も
7. ~~記録画面（`FUN_00414b00`）~~ 済み。得点の保存（レジストリの代わりに
   ページの localStorage）と `"rankin = %d rcurX = %02d"` の名前入力は
   まだ —— 点が入る所（本体）と一緒にやること
8. ~~`stage3.bin`~~ 読めた。**3 面（宇宙）の台本**だった（下記）。
   海と空の面の敵は `0x43fae8` の表、宇宙の面だけこのファイル
9. ~~録画の書き出し~~ 済み。状態 0x35 が `plat_write("demo1.dat", ...)`
   を呼ぶ（`FUN_004031d0` は fopen/fputc でそのまま書く）。検査は
   書かない（`disk/demo1.dat` は原作のもの）、ページは同じ名前で
   ダウンロードに出す
9. ~~当たり判定の枠~~ 済み（`play_box` = `FUN_00403520`、命令 0x84c）。
   艦は 0x40x0x20、敵は種類 1/2/4 が 0x40x0x20、3/9 が 0x20x0x20、
   爆雷は 0x10x0x10。**y に 0x20 を足さないので枠は絵の 0x20 上に出る**
9. ~~エンディング~~ 済み（`src/ending.c`、`tests/ending_check.c`）。
   3 画面ある。**入口はキーではなくメニュー**だった（上記）:
   * `FUN_00408650`（地球）: ending.dar を 2 枚読み、bgm14。地球は敵枠 0
     で、x は `900 - 幅` から `0x200 - 幅` まで 1 ずつ。自機は
     (0x80,0xc4) へ 1 ずつ。着いたら "Congratulation!!"（赤）と
     "Yamaboku find another earth."（水色）を出して 0x96 フレーム数え、
     CAST へ。レーダーには自機（0xb40 二つ）と**地球（0xb48 =
     ending.dar の earth192lader）**が出る
   * `FUN_00408a80`（CAST）: 生き物 20 匹。名前は `0x43f870` から
     16 バイトずつ（空白で中央そろえ済み）、絵は `0x43f9b0`、
     動きの種類は `0x43fa04`。**書庫は読み替えない**（ending.dar のまま）
     ので、絵は depth.dar 側から出る。1 匹は 16 画素ずつ入ってきて
     0xc0 で止まり 0x4a 待って 0x280 まで抜ける。`FUN_00408a40` は
     `FUN_0041b6f0` を素で呼ぶので**左上を留めた 2 倍**（`vid_pat_scale`
     の中央留めとは別。`vid_pat_scale_at` を足した）
   * `FUN_00414210`（スタッフロール）: staff.dar を 0x123 枚、`finst1`。
     台本は `0x44082c` に 2 int ずつ（0 = 空け、1 = 絵、2 = 文の組、
     0xff = 終わり）。2 フレームに 1 画素上へ、最後の行が 0x160 を
     切ったら 0x3c 待って名前入力へ。**フレーム数を "%5d" で
     (col 9, row 5) に出しっぱなし**（デバッグの名残。そのまま入れた）。
     0xac8 フレームで `finend2` に変える所があるが、
     **インストーラに finend2.mid が無い**（finst1.mid はある）
   * staff.dar のフォントは `kgfwhite`(2) / `kgfred`(98) /
     `kgfyellow`(194) の 96 枚ずつ、16x33。**引きかたが depth.dar と
     違って `bank + ASCII - 0x20`**（`FUN_00414140`。中央そろえも
     この関数が幅から計算する）
   * SOUND TEST（0x46）は入口が無いままなので移植しない

10. ~~ゲーム本体~~ 済み（`src/play.c`、`tests/play_check.c`）。
   面クリアの演出（`FUN_00408210`）と得点のポップアップ
   （`FUN_0040b6c0`/`FUN_0040b740`）も入れた。残りは:
   * ~~`FUN_0040c9e0`（空の面）~~ 済み（`src/air.c`、`tests/air_check.c`）。
     クリアの `FUN_0040f490` も。次は **`FUN_0040f970`（2058 行）=
     宇宙の面**（`LAB_0040110e`。`disk/depth4.jpg` / `depth5.jpg` の画面）。
     その先に `FUN_00403dc0`（917 行、`LAB_004011c7`）と
     `FUN_00408650`（120 行、`LAB_0040113b`）もある
   * ~~ゲームオーバーと名前入力~~ 済み（`play_over_frame`）。
     得点表の保存も入れた —— 原作はレジストリ（`rank%d` に 0x28 バイト）、
     ここは `sd_rank_ptr` / `sd_rank_stamp` を見てページが localStorage
     に hex で置く
   * ~~ポーズ画面~~ 済み（`play_pause_frame`）
   * 録画の書き出し（`FUN_004031d0`、状態 0x35）
10. ~~効果音~~ 半分済み。名前は上の表、ページが `disk/*.wav` を
   fetch して鳴らす（`sd_se_take`）。鳴らす場所は本体の移植と一緒

## 3 面（宇宙）—— `FUN_0040f970`（移植済み、`src/space.c`）

**移植した。** 下の下調べはそのまま読み方の説明として残す。

### 台本は `stage3.bin`

入口で `FUN_00413ae0`（星 96 個の初期化）と `FUN_00413df0`（台本の
読み込み）を呼び、**読めなければ状態 0x1e（タイトル）へ戻る**。

ファイルは 0x20 バイトの頭 + 24 バイトのレコード x N:

| 位置 | |
|---|---|
| +0x00 | `"SDEPTH"`（16 バイト） |
| +0x10 | 版 = 2 |
| +0x14 | 面 = 3 |
| +0x18 | 0x32a（一致しないと拒否） |
| +0x1c | レコード数 = 275 |

レコード（6 dword）は `[0]`=番号、**`+4` の 1 バイト目が種類、
2 バイト目が下位種類**、残りが引数。`FUN_00413df0` がこれを 6 dword の
実行時台本（`DAT_004b78e8`、24 バイト刻み）に書き直す:

| 種類 | 下位 | 実行時の型 | 意味 |
|---|---|---|---|
| 0 | | 0 | 何もしない |
| 1 | 1 | 3 | 敵を**乱数の位置に**出す（`value` = `[2]` が敵の kind） |
| 1 | 2 | 2 | 敵を**指定の位置に**出す（`[3]` `[4]` も渡す） |
| 2 | | 1 | 待つ（フレーム数 = `[5]`） |
| 3 | | 0x32 | **画面が空になるまで**待つ |
| — | | 0xff | 台本の終わり（最後の次に書かれる） |

実測: 275 本のうち種類 1 が 165、2 が 105、3 が 5。

### 進行

`DAT_004ba92c` が台本のカーソル、`DAT_004b78dc` が待ちフレーム。
0xff まで来て画面が空になると `"EMERGENCY"` を点滅させ、bgm15 を鳴らし、
`DAT_004ba934` が 0x45 を超えたところで**面番号を 1 増やして
`LAB_004011c7`（= `FUN_00403dc0`、4 面）へ**。

### 弾も敵配列に入る

**`FUN_00414060(kind, x, y, vx, vy)` が空きスロット（kind == 0）に
kind 0x14 / 0x15 を置く。** つまり 1..12 が敵の機体、0x14 以降が弾で、
**この面の空きの印は `kind == 0`**（海は y==0、空は y<=-0x20）。
だから `DAT_00463dac` が 0x40（64）になっている。

| kind | 絵 |
|---|---|
| 1..12 | 機体。それぞれ `DAT_00462204` を状態にした多段の動き |
| 0x14 | `anim + 0x987` |
| 0x15 | **(vx, vy) の向きで 16 方向** `0xb1b + 0..15` |
| 10 / 0xc | `0xac6` / `0x44072c` の表 + `0xb06`。`DAT_004621a0` が立っていると `FUN_00409090`（別の描画）で描く |

機体が使う欄（`DAT_004621a8` を x として）: `+0x44` 耐久、`+0x4c` と
`+0x5c` 汎用の数え、`+0x58` anim、`+0x64` 状態、`+0x00`（= x の 8 バイト前）
描画の切り替え。**レコードは 0x4621a0 から 112 バイト**で、海と空で
使っている欄はその一部。

### 当たり判定は種類ごとに箱の大きさが違う（`FUN_0040b850`）

`FUN_0040b850(x1,y1,x2,y2,w1,h1,w2,h2)` が「箱の隅が入っているか」を
4 通り調べる。宇宙の面はこれを 3 か所で使う:

| 相手 | 自機の弾（10x12） | 大玉（0x40x0x20） | 自機（0x20x0x14） |
|---|---|---|---|
| kind 1 | 0x20x0x20、`+0x14` が 0 でなければ | 同じ | 同じ |
| kind 2/3 | 0x40x0x20 | 同じ | 同じ |
| kind 4 | 0x80x0x40、状態 2/3 のときだけ | 同じ | 同じ |
| kind 5 | 0x40x0x40、`+0x4c` が 0xff 以上のときだけ | 0x40x0x20 | 0x40x0x40 |
| kind 6 | 0x20x0x20、状態 1 のときだけ | 同じ | 0x40x0x20 |
| kind 7/8 | 0x20x0x10 | 同じ | 同じ |
| kind 9 | 0x20x0x20（**倒すとアイテム**） | 0x40x0x20 | 0x20x0x20 |
| kind 10/0xc | 0x20x0x20、耐久を 1 減らす | 同じ | 同じ |
| kind 0x14/0x15 | 0x10x0x10 | 同じ | 8x8 対 0x3cx0x18 |

**耐久（`+0x44`）が 0 になったときだけ死ぬ**種類がある（4, 5, 10, 0xc）。
減っただけのときは `depth06` を鳴らして `+0x00` を 1 にする
（次のフレームだけ別の絵で描く = 光る）。

### 自機の大玉（`0x4bb148` から 64 枠）

`DAT_004ba938` が残っているあいだ、**3 フレームごとに 1 個**、
画面左（x = -0x40）から y = 乱数、vx = 0x18 + 乱数%4 で飛ばす。
`finalt09.wav` が鳴り、`DAT_004ba930 = 1`（画面が 1 フレーム白くなる）。
絵は `0xaa0` と `0xaa1` を横に並べたもの。x が 0x25f を超えたら消える。
枠は 28 int 刻みで、+0x00 x、+0x04 y、+0x08 vx、**+0x34 が生きている印**。

### 描画は 3 つ足した（済み）

`src/video.c` に入っている:

| | |
|---|---|
| `FUN_00409090(surf,x,y,pat)` | **当たった敵の白い影**。ふつうの blit に翻訳表 `DAT_0050baf8` を渡すだけで、その表は `FUN_00431de0(tbl, 0, 0xfe, 0xff)` で作られている —— **0..0xfe を全部 0xff にする**表。つまり絵の形を色 0xff で塗る。10 行で書ける |
| `FUN_00409120(surf,x,y,pat,sx,sy)` | **拡大縮小 blit**（kind 5 が 0x10 → 0x100 と大きくなって現れる）。位置は絵の中心を保つように補正される。中身は `FUN_0041b6f0`（縦の行の間引きと複製）＋ `FUN_0041b850`（横、131 行の走リストの走査。倍率 0x100 以下・0x400 以下・それ以上で 3 通り）。**ここは真面目に読まないと合わない** |

波打ち blit（`FUN_004092a0`）は `vid_pat_wave` として入っている。

### 描くのは 2 周（前後がある）

`DAT_004ba928` を 0 → 1 と回し、**敵の `+0x64`（`DAT_0046220c`）が
その周と同じものだけ描く**。だから大きい敵の前後に小物が入る。

* 死んでいる（state < 10）敵の爆発は種類で位置がずれる。
  1/6..0xc は `(x-0x10, y-0x10)`、2/3 は `(x, y-0x10)`、4 は
  `(x, y)` と `(x+0x40, y)` の 2 つ、5 は `(x, y)`
* **弾（0x14/0x15）の爆発は `FUN_0040ae90(x, y, 3-state)`** という別の
  4 コマ。state が 4 より大きいときは 3 に丸める
* **kind 4（大物）は `FUN_004092a0` の波打ち描画**で 4 枚に分けて描く
  （`0xa84`/`0xa85`/`0xa86`…、振幅は `+0x4c` の下位バイト、位相は anim）。
  状態（`+0x64`）で使う絵が変わる

### 狙い撃ちは自機の歩幅ぶん先を撃つ（`DAT_00461340` / `DAT_0046133c`）

種類 2/3 が撃つ弾（`FUN_00414060(0x14, ...)`）の x 成分は
**`+ DAT_00461340`**（= その frame の艦の歩幅 `pdx`）。ところが
`DAT_00461340` を書くのは**海と空の面だけ**なので、宇宙の面が足すのは
「前の面が残していった歩幅」。y 成分も同じ形で `DAT_0046133c` を足すが、
**この番地に書く命令は binary 中に一つも無い**（.data の 0 のまま）。
つまり縦には先を読まない。そのまま移した（`src/space.c`）。

### そのほか（宇宙の面）

* **アイテム 4 は Flash Bomb ではなく `DAT_004ba938 += 0x3c`** ——
  自機が大玉を撃つ時間が伸びる。ほかのアイテムは海と同じ `FUN_0040aab0`
* アイテムは `+8` ずつ左へ流れて（`DAT_00463db4` が減る）、x が 0 を
  切ると消える
* `FUN_0040b080(n)` が**星の流れ（`DAT_004a8a50` / `DAT_004a8a54`）を
  変える** —— 上下キーで縦、8 フレームごとに横。星は画面を回り込む

### 星は 2 層

* `0x4a8a40`（4 int 刻み x 64）—— 空の面のクリアの雨と同じ配列を使う
* `0x4ba940`（8 int 刻み x 64、`FUN_00413ae0`）—— x, y, vy = -0x1c-乱数%8,
  そして種類 %3 / %15 / %2 / %2 の 4 つ

### そのほか

* 自機は**上下にも動く**（x 0x20..0x220、y 0..0x140）。速さは最低 4
* **BTN1 は左へ、BTN2 は右へ撃つ**（`(-2-powerB)*6` と `(powerB*3+6)*2`）。
  y は `py + 0xc` 固定。3 way は `+0x24` に -1/0/1
* 自機の噴射は `0xa90` / `0xa91` を `(px+8, py+8)` と `(px+0x28, py+8)` に
* `DAT_004ba930` が残っていると**奇数フレームだけ画面を 0xff で塗る**
* 星は `FUN_00413ae0` が 96 個、`0x4ba954` から 8 dword 刻み
* `0x4bb17c` の 64 枠は `0xaa0` と `0xaa1` を横に並べて描く何か

## 4 面（移植済み、`src/boss.c`）と、届かないもの

* **4 面 = `FUN_00403dc0`（917 行、`LAB_004011c7`）**。space.dar、
  入口で白フラッシュ 6、bgm06。敵枠 0 が大物（絵は depth.dar の
  `boss1` = 0xb2d）で、**弱点は真ん中の帯（y+0x1c..y+0x34）だけ**。
  12 発までは左右を往復して 4 つの口から弾を出し、それを越えると
  艦を追いかけて爆弾を落とす。30 発で崩れ、0x78 フレームの爆発のあと
  8 つに割れて（表 `0x43f6d4` / `0x43f71c`）bgm11 と "Destroyed!"、
  0x1b8 フレームで**面番号を 1 に戻して海の面へ**。
  得点は表の 4 行目（倒して 1000、1 発 10）
* **`stage / 4 == 1` のときだけ当たる。** つまり 8 面と 12 面の大物は
  弾も当たらず体当たりもしない（beta の癖。そのまま移した）
* **エンディング = `FUN_00408650`（120 行、`LAB_0040113b`）**。
  ending.dar（`earth192`）と bgm14、自機が (0x80, 0xc4) へ寄っていく。
  **この build では面クリアからは入れない** —— 下の面選びだけが入口
* **面選び（debug）**: 0x426100..0x4266d1 に、状態が 0x32/0x33/0x34 の
  ときに面番号と hook を直に入れる分岐が並んでいる（Ghidra は関数として
  認識しない）。ここから読める対応が
  **1/5/9 = 海、2/6/10 = 空、3/7/11 = 宇宙、4/8/12 = 4 面**。
  `DAT_00462198`（= (面-1)%4+1）の 4 周期はこれ

## 取扱説明書（`disk/index.html`）—— 作者自身が書いた仕様

インストーラに入っている HTML の説明書。**移植の裏を取るのに使える
一次資料**なので、疑問が出たらまずここを見る（Shift_JIS。UTF-8 に
直して読む）。読み取れる事実:

* **アイテムは色で 7 種類**。binary の名前（`0x44010c`）と並べると:

  | 番号 | 説明書 | binary の名前 | 移植での中身 |
  |---|---|---|---|
  | 1 | 青色 スピードアップ | `Speed Up` | `speed`（2..8） |
  | 2 | 赤色 連射数アップ | `Shot Max Up` | `charges`（4..0x10） |
  | 3 | 緑色 弾のスピードアップ | `Shot Power Up` | `powerB`（爆雷が速く沈む） |
  | 4 | 黄色 フラッシュボム | `Flash Bomb` | 画面の敵を全部倒す |
  | 5 | 紫色 3WAY SHOT（**海面では WAVE BOMB**） | `Shot Special` | `powerA`（海では爆雷が揺れる、他では 3 発） |
  | 6 | 水色 フルパワー | `Full Power` | 全部最大 |
  | 7 | 白色 1UP | `Ship 1up` | `lives++` |

* **「海面と空面では横移動しかできません」** —— 上下は宇宙面と大物面だけ
* **「ある特定のキャラ(レーダー上では白く点滅)をやっつけるとアイテムを
  出す」** —— 種類 9（`0x9c9`）。レーダーで奇数フレームだけ 0xb40 を
  出すのがこれ
* **「全12面をクリアーするとエンディングが用意されています。
  でも現在は４面までしか遊べません」** —— エンディングが遊んでは
  出せないことは**作者自身が書いている**。大物を倒すと面番号が 1 に
  戻る（`FUN_00403dc0` の `DAT_00463da4 = 1`）のは、そのための仮の作り
* 艦の名前は「ヤマ卜（ぼく）」。だからエンディングの
  "Yamaboku find another earth." になる
* WinGL L1 Version 0.11e と TSW WinGL Prototype LEVEL3 を使っている
  （スタッフロールに出てくるとおり）

## 原作の録画（`disk/demo1.dat`）が一番良い検査になる

**8235 フレームの pad の記録で、原作の上で録られたもの。**だから
「記録どおりに押したら原作と同じことが起きるか」を丸ごと試せる。
移植が合っていれば、記録が終わるまで（8235 フレーム）沈まずに進み、
海 → 空 → 宇宙 → 大物 まで行って**記録切れで logo に戻る**。
`tests/play_check.c` がそれを見ている:

```
  the recording: 8235 of 8235 frames read, score 4622, stage 4,
                 4 hits, out at frame 8234 (state 10)
```

**この検査が連鎖の取り違えを見つけた。** それまでは海の面の 2584
フレーム目で 3 回沈んで終わっていた（kill 10、記録の 31%）。原因は
`DAT_00463da8`（連鎖の入り切り）を `Play` の欄にしていたこと ——
**あれは `FUN_00402480`（窓を作る所）が起動時に 1 にして誰も戻さない
globalで、`FUN_0040b250` が消す範囲の外**。`Play` に置いたので
`play_clear` が毎回 0 にしてしまい、**連鎖が一度も起きていなかった**。
`Game` に移して起動時に 1 にしたら、記録は最後まで通った
（kill 274、得点 4622、4 面まで）。

教訓: **入れ物を間違えると値が消える。** 「どの構造体に置くか」は
好みではなく、原作がどこで初期化してどこで消すかで決まる。

## 突き合わせのやりかた（global を機械的に照合する）

「移植で落ちている枝」を見つけるのに効いた手。逆コンパイルの関数本体を
切り出して、出てくる `DAT_*` / `LAB_*` の番地を数え、移植側のソースと
RESUME に**その番地が文字列として出てくるか**を見る。出てこないものだけ
並べて、次の 3 種類を除くと残りは数個になる:

* コードラベル（0x401000..0x420000）
* パターン表 `0x470bd8 + 番号 * 0x34`（幅）と `+2`（高さ）
* 28 int の配列（敵 `0x4621a8`、爆雷 `0x4a5488`、砲弾 `0x4a5b88`、
  魚雷 `0x4a4d80`、しぶき `0x4a5fa0`、得点ポップアップ `0x4a8548`、
  空の爆弾 `0x463dd0`、`0x461a68`、`0x461350`）の base + 中の offset

道具は `tools/globals.py`（`python tools/globals.py FUN_0040f970`）。
これで**海・空・宇宙・大物・エンディング・CAST・スタッフロール・
面クリア 2 種・名前入力・ポーズ・記録画面・メニュー・状態機械**を
ぜんぶ通した。説明のつかない番地は残っていない。
見つかったのは狙い撃ちの `DAT_0046133c`（上記）1 件。

## Ghidra（この機械に入っている）

パスはグローバルの `~/.claude/CLAUDE.md` にも控えてある。
**プロジェクトのディレクトリは先に作っておくこと**（無いと
`Directory not found` で落ちる）:

```sh
export JAVA_HOME='C:\prog\ghidra\jdk-21.0.12.1+1'
GH="C:\prog\ghidra\ghidra_12.1.3_PUBLIC\support\analyzeHeadless.bat"

mkdir -p ghidra_proj out
cmd //c "$GH" ghidra_proj sd -import disk/superdepth.exe -overwrite     # 50 秒
cmd //c "$GH" ghidra_proj sd -process superdepth.exe -noanalysis \
    -scriptPath tools/ghidra -postScript DumpAsm.java out/superdepth.asm
cmd //c "$GH" ghidra_proj sd -process superdepth.exe -noanalysis \
    -scriptPath tools/ghidra -postScript MakeThunkFuncs.java   # 表の飛び先
cmd //c "$GH" ghidra_proj sd -process superdepth.exe -noanalysis \
    -scriptPath tools/ghidra -postScript DumpAll.java out/superdepth.c
```

`ghidra_proj/` と `out/` は .gitignore。DumpAll は 1 分ほど（16bit の
soko_ban では 15 分かかったが、こちらは速い）。

## 作業のしかた（守ること）

* **GUI を開かない。** PNG に描いて `Read` で見る
* **WebGL 禁止**（対象機に無い）。ソフトで描く
* **インストーラを走らせない。** 中身は解いて取り出す（もう解けている）
* **推測で埋めない。** 分からないところは `out/superdepth.c` を読み直すか、
  分からないと書く。「教科書どおり」は根拠ではない（soko_ban で
  プレーンとビットの対応をそれで間違えた）
* Bash のヒアドキュメントは**バックスラッシュを食う**。python のパッチは
  Write でファイルに書いてから走らせる
* ビルドは MSVC 直叩き（`tools/cc.sh`）。node は PATH に無い:
  `PATH="/c/prog/emsdk/emsdk/node/22.16.0_64bit/bin:$PATH"`

## この game について

Bio_100% の『SuperDepth』（1991、PC-98）の Windows 移植のベータ版。
Build 115 = 1999-02-14。`readme.txt` によると

* Win95 以降 / NT4 SP3 以降、256 色以上・640x480 以上
* **ベータなので 5 面以降は「遊べないステージばかり」**（Build 114 の note）
* オンラインアップデート機能つき（`ipatcfg.exe`、
  `http://bio.and.or.jp/cgi-bin2/sdepth`）
* HTML のマニュアルが同梱（`index.html`）

PC-98 版の移植 [super_depth_wasm](https://github.com/yomei-o/super_depth_wasm)
が別にある。**同じ game の別実装**なので、面データやパターンの並びは
比べる価値がある（あちらの docs が使えるかもしれない）。
